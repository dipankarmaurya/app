<link rel="stylesheet" href="../assets/CSS/userStyle.css">
    <div id = "user-profile">

    
    <h1>
        user profile
    </h1>
    <p> name =<%= profile_user.name%></p>
    <p>email:<%= profile_user.email%></p>
    <%if(user.id==profile_user.id){%>
       <h3>Update User</h3>
       <form action="/users/update/<%= profile_user.id %>" method="POST">
        <input type="text" name="name" placeholder="change Name" value="<%= profile_user.name%>" required>
        <input type="email" name="email" placeholder="change Email" value="<%= profile_user.email%>" required>
         <input type="submit" value="Update"> 
    </form>
    <%}%>
   
</div>
